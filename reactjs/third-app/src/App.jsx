// React hook = special function that allows functional components to use react features without writing class components

import MyComponent from "./MyComponent"

// useState = A react hook that allows the creation of stateful variable and a setter function to update its value in the DOM

function App() {

  return (
    <>
      <MyComponent />
    </>
  )
}

export default App
